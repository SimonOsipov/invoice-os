{
  "name": "@invoice-os/e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "E2E suites against the deployed fleet: three Playwright projects (smoke = SPA render + console coverage + persona boundaries, api = typed HTTP contract suite over the gateway, topology = browser-verified gateway round trips) plus deployment-free vitest unit tests for this package's own seams. See e2e/README.md.",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test",
    "test:topology": "playwright test --config playwright.topology.config.ts",
    "test:api": "playwright test --config playwright.api.config.ts",
    "test:unit": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@invoice-os/api-client": "workspace:*",
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
