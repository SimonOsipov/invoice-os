// LAND-04-02 RED-stage stub. Typed constants + trivial markup only, so
// Privacy.test.tsx fails on assertions, not module resolution. Real copy and
// structure land in the GREEN commit — see task-556.
export const GA_RETENTION_MONTHS = 0
export const PROSE_MAX_WIDTH = 0
export const PRIVACY_CONTACT = 'PRIVACY_CONTACT_ADDRESS_NOT_YET_SUPPLIED'
export const ANALYTICS_DEFAULT_SENTENCE = 'placeholder'

export function Privacy() {
  return <div />
}
