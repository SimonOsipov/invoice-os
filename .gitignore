# ---- Go ----
# Compiled service binaries from cmd/* (built into ./bin or in-place)
/bin/
*.exe
*.test
*.out
coverage.*
# Compiled tool binary emitted at repo root by `go build ./internal/tools/rlsgate`
/rlsgate

# Go workspace file (machine-local)
go.work
go.work.sum

# ---- Frontend (Node) ----
node_modules/
frontend/**/dist/
frontend/**/build/
frontend/**/.vite/
frontend/**/.verify/
*.tsbuildinfo

# ---- Python (sidecar/docling) ----
__pycache__/
*.pyc
.venv/

# ---- Playwright (e2e smoke suite) ----
e2e/test-results/
e2e/playwright-report/
# Per-suite reports parked by dev-env.yml before the next suite overwrites
# playwright-report/ (the three artifacts used to all hold the last suite's run).
e2e/report-smoke/
e2e/report-api/
e2e/report-topology/
e2e/blob-report/
e2e/.playwright/

# ---- Secrets / env ----
.env
.env.*
!.env.example

# ---- Backlog.md (local task tracking, not versioned) ----
backlog/

# ---- Claude Code (local project instructions, not versioned) ----
CLAUDE.md
# RALPH story worktrees live here (see RALPH_PROMPT.md) — never commit them
.claude/worktrees/
# RALPH per-worktree orchestration scratch (session state, not repo content).
# Untracked-but-not-ignored kept getting swept into `git add -A` by agents.
.claude/handoff.yaml
# Playwright-MCP screenshot scratch — regenerable captures from browser QA runs,
# never repo content. Same failure mode as handoff.yaml above: 15 of these were
# swept into f0cf3bf (a one-line comment fix) by `git add -A` before this rule
# existed, and /post-merge-cleanup deletes the directory on the documented
# assumption that it is ignored.
.playwright-mcp/
# RALPH per-story scratch (design notes, checkpoints), never repo content.
.ralph/

# ---- Editor / OS ----
.DS_Store
.idea/
.vscode/
*.swp
