// Numeric timing constants for the switch's busy beat and toast, kept out of copy.ts
// (copy.ts is scanned by copy.test.ts as "every export is rendered copy").

// The floor App holds the busy beat open for, regardless of round-trip speed.
export const BUSY_MS = 700
// How long the toast stays up before auto-dismissing.
export const TOAST_MS = 5200
