-- Scaffold only: the repair bodies land with the implementation. The tests in
-- internal/platform/db/demo_repair_migration_test.go are RED against this file.

-- +goose Up
SELECT 1;

-- +goose Down
SELECT 1;
