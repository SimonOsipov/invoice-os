// The seed policy trees, behind a stable import path. A re-export ONLY at this subtask —
// APPR-09-03 moves the literals into this body when lib/workflows.ts stops shipping them.

export { SEED_FIRM_POLICIES, SEED_INHOUSE_POLICIES, seedPolicies, type PolicyStore } from './workflows'
